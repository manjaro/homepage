<section>
    <div class="container">
        <div class="row">
            <div class="news-post text-center" >
               
                <h3><a href="{{ .Params.sponsorurl }}" target="_blank">{{ .Params.name }}</a></h3> 
                {{ with $.Params.image }}
				<img src="{{ $.Site.BaseURL }}/img/sponsor/{{ . }}" alt="" />
				{{ end }}        
				
				<p>... {{ .Params.reason }}</p> 
                <hr />   
                </div>
            </div>
        </div>
    </div>
</section>