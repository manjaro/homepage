{{ "<!-- NAVIGATION -->" | safeHTML }}

<nav id="download-nav" class="navbar subnavbar">
    <div class="container-fluid">
        {{ "<!-- Brand and toggle get grouped for better mobile display -->" | safeHTML }}
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <ul class="nav navbar-nav">
            	<li>
                    <a class="page-scroll" href="{{ .Site.BaseURL }}download">Editions finder</a>
                </li>
            </ul>
        </div>
		
        
        {{ "<!-- Collect the nav links, forms, and other content for toggling -->" | safeHTML }}
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">	
            	
            	{{ range .Site.Pages }}{{ if eq .Type "download-edition" }}{{ if in .Params.tags "official" }}
            	
				<li>
                    <a class="page-scroll" href="{{ .Permalink }}">{{ .Params.name }}</a>
                </li>            	
            	
            	{{ end }}{{ end }}{{ end }}
            	
            	<!-- While HUGO is good, the documentation is really, really, really bad, confusing and does explain everything except the things you want to know. I was not able to find an operator for 'Not in' list -->
            	<li class="dropdown">
			      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Manjaro Community Editions <span class="caret"></span></a>
			      <ul class="dropdown-menu">
			      	{{ range .Site.Pages }}{{ if eq .Type "download-edition" }}{{ if in .Params.tags "official" }}
			      		{{ else }}
			      		<li style="min-width: 200px;"><a class="page-scroll" href="{{ .Permalink }}">{{ .Params.name }}</a></li>
			      	{{ end }}{{ end }}{{ end }}
			      </ul>
			    </li>              
               
            </ul>
        </div>
        {{ "<!-- /.navbar-collapse -->" | safeHTML }}
    </div>
    {{ "<!-- /.container-fluid -->" | safeHTML }}
</nav>
